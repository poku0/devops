# Creating Python virtual environment (venv)

## Setting up venv, activating it, installing libraries

```
Create Python virtual environment
python3 -m venv venv

Activate it:
. venv/bin/activate

Create requirements.txt that contains the required packages/libraries

Install them:
pip install -r requirements.txt

Exit venv:
deactivate
```
