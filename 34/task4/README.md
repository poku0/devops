# Task

## Create a Flask application that uses Jinja2 template inheritance. Make a base template that includes common elements like a header and footer, then create two other pages that inherit from this base template and ad their own body content. 

